// import { isCurrentUserAdmin } from '../03-mocking-modules';

/*
  In this test we have a function that depends on another module result
  please uncomment the test below and mock the module isCurrentUserAdmin function uses
  to get the currentUser by replacing it not be an admin user.

  To learn more about mocking modules (also known as manual mocks) please see the following page:
  https://facebook.github.io/jest/docs/en/manual-mocks.html
 */

test('mocking current user');
